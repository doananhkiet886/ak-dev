<div class="heading d-flex justify-content-between">
  <h2 class="">My Courses</h2>
  <div class="btn-wrap">
    <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#add-new-course-modal">Add new course</button>
    <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#edit-course-modal">Edit course</button>
  </div>
</div>
  
<table class="table table-striped table-bordered mt-4">
  <thead class="table-dark">
    <tr>
      <th scope="col">#</th>
      <th scope="col">ID</th>
      <th scope="col">Name</th>
      <th scope="col">Image ID</th>
      <th scope="col">Video ID</th>
      <th scope="col">Create At</th>
    </tr>
  </thead>
  <tbody>
    <% for(let i = 0; i < courses.length; i++) { %>
      <% const course = courses[i] %>
      <tr>
        <th scope="row"><%= i + 1 %></th>
        <td><%=  course._id %></td>
        <td><%= course.name %></td>
        <td><%= course.imageId %></td>
        <td><%= course.videoId %></td>
        <td><%= moment(course.createdAt).format('YYYY/MM/DD hh:mm:ss A')  %></td>
      </tr>
    <% } %>
  </tbody>
</table>

<%- include('./addNewCourseModal.ejs') %>
<%- include('./editCourseModal.ejs') %>